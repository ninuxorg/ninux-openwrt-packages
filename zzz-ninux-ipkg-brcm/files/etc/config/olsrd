config olsrd
	# uncomment the following line to use a custom config file instead:
	#option config_file '/etc/olsrd.conf'

	option IpVersion '6and4'

config LoadPlugin
	option library 'olsrd_mdns.so.1.0.0'
	option "NonOlsrIf"  "eth1"

config Interface
	list interface 'wl0'
