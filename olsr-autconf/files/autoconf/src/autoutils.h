#ifdef OLSR_AUTOCONF_AUTOUTILS
#define OLSR_AUTOCONF_AUTOUTILS

/* Configure Interfaces (others than if2skip) in ad-hoc mode and configure
 * Ipv4 address to saved_if (if any) or to some bytes taken from mac hardware.
 */

int autoconf_interfaces(char* if2skip, char* saved_if){


return 0;
}

/* Change the interface address upon a collision is detected
 */

int change_if(char* if){}





#endif
